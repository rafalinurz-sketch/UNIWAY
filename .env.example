# Public — safe to expose in the browser. This is Supabase's PUBLISHABLE (anon)
# key: your data is protected by Row Level Security policies, not by hiding this key.
NEXT_PUBLIC_SUPABASE_URL=https://ummjmbqrnbqzxvfxptru.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_tmElsnpS5zjEuEQ42EDtSg_c4cjk4g9

# Optional — only used by server-side API routes (app/api/ai/route.ts) to power
# the AI Advisor, essay feedback, speaking feedback and study-plan generation.
# Never prefix this with NEXT_PUBLIC_ — it must stay server-side only.
# Leave unset and every AI feature will say plainly "AI not connected" instead
# of faking a response.
ANTHROPIC_API_KEY=

# NEVER put your Supabase service_role/secret key in this file if this file is
# committed to git or deployed to Vercel as a NEXT_PUBLIC_ variable. If a future
# server-side-only feature needs it, add it as SUPABASE_SERVICE_ROLE_KEY in
# Vercel's Environment Variables dashboard directly (not in this checked-in
# file), and only read it from server code (API routes / Edge Functions),
# never from a "use client" component.
